---
description: Create a worktree for non-issue work (docs, config, small fixes)
argument-hint: <name> [slug]
---

Run `bash .claude/scripts/start-task $ARGUMENTS` from the repo root, then work inside the printed worktree. When done, run `/auto-pr` from inside it.
